if ! command -v pveversion >/dev/null 2>&1; then
  echo "Error: must run on Proxmox host"
  exit 1
fi
source <(curl -fsSL "$REPO_BASE/misc/core.func") 2>/dev/null
source <(curl -fsSL "$REPO_BASE/misc/api.func") 2>/dev/null
source <(curl -fsSL "$REPO_BASE/misc/error_handler.func") 2>/dev/null
