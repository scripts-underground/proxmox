if ! grep -q container=lxc /proc/1/environ 2>/dev/null; then
  echo "Error: must run inside LXC container"
  exit 1
fi
source <(curl -fsSL "$REPO_BASE/misc/core.func") 2>/dev/null
source <(curl -fsSL "$REPO_BASE/misc/tools.func") 2>/dev/null
